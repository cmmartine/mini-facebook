<turbo-frame id="new_comment" target="_top">
  <%= form_with model: [@post, @comment], local: true do |f| %>
    <%= f.text_area :body, size: "50x3", class: "comment-form" %>
    <%= f.submit "Submit comment" %>
    <%= f.hidden_field :user_id, value: current_user.id %>
  <% end %>
</turbo-frame>
